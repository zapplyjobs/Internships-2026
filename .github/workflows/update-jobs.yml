name: Update Zapply Jobs

on:
  schedule:
    # Run every 15 minutes
    - cron: '*/15 * * * *'
  workflow_dispatch: # Allow manual triggering
  push:
    branches: [ main ]
    paths:
      - '.github/scripts/job-fetcher/**'
      - '.github/scripts/real-career-scraper.js'
      - '.github/scripts/enhanced-discord-bot.js'

# Prevent concurrent runs to avoid queue corruption
concurrency:
  group: job-updates-${{ github.repository }}
  cancel-in-progress: false

jobs:
  update-jobs:
    runs-on: ubuntu-latest
    timeout-minutes: 30  # Prevent stuck runs from blocking future runs
    permissions:
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        ref: ${{ github.ref_name }}  # Use the branch that triggered the workflow
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: |
          package-lock.json
          jobboard/package-lock.json

    - name: Install job fetcher dependencies
      run: npm install axios cheerio

    - name: Install scraper dependencies
      run: |
        npm install puppeteer
        npx puppeteer browsers install chrome

    - name: Install jobboard dependencies
      run: |
        cd jobboard
        npm install --production --no-audit --no-fund
        cd ..

    - name: Create logs directory
      run: mkdir -p .github/logs

    - name: Update job listings
      run: node .github/scripts/job-fetcher/index.js 2>&1 | tee .github/logs/job-fetcher.log
      env:
        JSEARCH_API_KEY: ${{ secrets.JSEARCH_API_KEY }}
        # Database retention window (14 days to catch slow-updating sources)
        ACTIVE_WINDOW_DAYS: 14
        # Feature flag: Use tagged aggregator feed instead of direct JSearch API
        # ABANDONED: Using individual JSearch per repo (2026-02-06)
        # Aggregator complexity outweighs benefits - independence preferred
        USE_AGGREGATOR: 'false'

    - name: Display job-fetcher logs (always run)
      if: always()
      run: |
        echo "📋 Job Fetcher Output (last 100 lines):"
        tail -100 .github/logs/job-fetcher.log || echo "Log file not found"


    - name: Display job-fetcher summary
      run: |
        echo "📊 Job Fetcher Summary:"
        echo "========================"
        if [ -f ".github/logs/job-fetcher.log" ]; then
          # Extract key metrics from log
          echo "✅ Jobs found:" $(grep -o "Found [0-9]* jobs" .github/logs/job-fetcher.log | tail -1 || echo "0 jobs")
          echo "✅ After filters:" $(grep -o "After.*filter.*[0-9]* jobs" .github/logs/job-fetcher.log | tail -1 || echo "Unknown")
          echo "🔍 Errors:" $(grep -c "Error\|error\|ERROR" .github/logs/job-fetcher.log || echo "0")
          echo "⚠️  Warnings:" $(grep -c "Warning\|warning\|WARN" .github/logs/job-fetcher.log || echo "0")
          echo ""
          echo "Last 10 lines of log:"
          tail -10 .github/logs/job-fetcher.log
        else
          echo "⚠️  No job-fetcher log found"
        fi

    - name: Export jobs to encrypted JSON (for external job boards)
      run: node .github/scripts/jobs-data-exporter.js
      env:
        LOG_ENCRYPT_PASSWORD: ${{ secrets.LOG_ENCRYPT_PASSWORD }}

    - name: Write current_jobs.json for aggregator
      run: node .github/scripts/write-current-jobs.js

    - name: Generate metrics report
      if: always()
      run: node .github/scripts/generate-metrics-report.js --days=7
      env:
        LOG_ENCRYPT_PASSWORD: ${{ secrets.LOG_ENCRYPT_PASSWORD }}

    - name: Check for changes
      if: always()
      id: verify-changed-files
      run: |
        if [ -n "$(git status --porcelain)" ]; then
          echo "changed=true" >> $GITHUB_OUTPUT
          echo "Found changes in README"
        else
          echo "changed=false" >> $GITHUB_OUTPUT
          echo "No changes detected"
        fi
      
    - name: Commit updated job board
      if: always() && steps.verify-changed-files.outputs.changed == 'true'
      run: |
        git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add -A
        
        # Get job count from README for commit message
        JOB_COUNT=$(grep -o "Active Positions\*\*: [0-9]*" README.md | grep -o "[0-9]*" || echo "0")
        COMPANY_COUNT=$(grep -o "Companies\*\*: [0-9]*" README.md | grep -o "[0-9]*" || echo "0")
        
        git commit -m "Update jobs - $(date +'%Y-%m-%d')
        
        Found $JOB_COUNT positions from $COMPANY_COUNT companies
        Updated categories, locations, and experience levels
        Next update tomorrow"
        
    - name: 🔄 Pull latest changes
      if: always() && steps.verify-changed-files.outputs.changed == 'true'
      run: |
          echo "📥 Pulling latest changes with rebase (ours strategy) to avoid conflicts..."
           git fetch origin ${{ github.ref_name }}
           git rebase origin/${{ github.ref_name }} --strategy-option=ours || git merge -X ours origin/${{ github.ref_name }} --no-edit
           echo "✅ Pull completed successfully!"


    - name: 🚀 Push updated changes with retry logic
      if: always() && steps.verify-changed-files.outputs.changed == 'true'
      run: |
        echo "🚀 Pushing changes with retry logic..."
        for i in 1 2 3; do
          echo "Push attempt $i of 3..."
          if git push origin ${{ github.ref_name }}; then
            echo "✅ Successfully pushed changes!"
            break
          else
            echo "⚠️ Push failed — pulling latest changes before retry..."
            git pull origin ${{ github.ref_name }} --rebase --autostash || git pull origin ${{ github.ref_name }} --no-edit
            sleep 5
          fi
        done
        
    - name: Create job summary
      run: |
        echo "## 🚀 Zapply Jobs Updated!" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        if [ "${{ steps.verify-changed-files.outputs.changed }}" == "true" ]; then
          # Extract stats from README
          JOB_COUNT=$(grep -o "Active Positions\*\*: [0-9]*" README.md | grep -o "[0-9]*" || echo "0")
          COMPANY_COUNT=$(grep -o "Companies\*\*: [0-9]*" README.md | grep -o "[0-9]*" || echo "0")
          FAANG_COUNT=$(grep -o "FAANG+ Jobs\*\*: [0-9]*" README.md | grep -o "[0-9]*" || echo "0")
          
          echo "✅ **Successfully updated job board with fresh opportunities**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📊 Today's Haul:" >> $GITHUB_STEP_SUMMARY  
          echo "- 🎯 **$JOB_COUNT total positions** from elite tech companies" >> $GITHUB_STEP_SUMMARY
          echo "- 🏢 **$COMPANY_COUNT companies** tracked (FAANG, unicorns, startups)" >> $GITHUB_STEP_SUMMARY
          echo "- ⭐ **$FAANG_COUNT FAANG+ jobs** from top-tier companies" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔍 Search Coverage:" >> $GITHUB_STEP_SUMMARY
          echo "- 🌐 **Multi-location search**: SF Bay Area, NYC, Seattle, Austin, Remote" >> $GITHUB_STEP_SUMMARY
          echo "- 🎓 **All experience levels**: Entry-level to Senior positions" >> $GITHUB_STEP_SUMMARY
          echo "- 💼 **10+ role categories**: SWE, ML/AI, Data, Mobile, DevOps, Product, Design" >> $GITHUB_STEP_SUMMARY
          echo "- 🏆 **Elite companies only**: FAANG, unicorns, top startups, gaming leaders" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🎯 Quality Filters Applied:" >> $GITHUB_STEP_SUMMARY
          echo "- ✨ Removed duplicates and spam postings" >> $GITHUB_STEP_SUMMARY
          echo "- 🏢 Normalized company names and subsidiaries" >> $GITHUB_STEP_SUMMARY  
          echo "- 📊 Ranked by company tier (FAANG first)" >> $GITHUB_STEP_SUMMARY
          echo "- 🔗 Verified direct application links" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**🔄 Next update**: Tomorrow at 9 AM UTC" >> $GITHUB_STEP_SUMMARY
        else
          echo "ℹ️ **No new opportunities found - job board is current**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "All tracked companies have been checked for new postings." >> $GITHUB_STEP_SUMMARY
          echo "The existing job listings are still fresh and active." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**🔄 Next check**: Tomorrow at 9 AM UTC" >> $GITHUB_STEP_SUMMARY
        fi
